/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "assets.coingecko.com",
      "cryptocompare.com",
      "resources.cryptocompare.com",
    ],
  },
};

module.exports = nextConfig;
